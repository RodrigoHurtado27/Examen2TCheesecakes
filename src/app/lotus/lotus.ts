import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-lotus',
  standalone: true,
  imports: [RouterLink],
  templateUrl: './lotus.html',
  styleUrl: './lotus.css',
})
export class Lotus {

}
